<h1>Sign In with Existing Account</h1>

<%= form_for @user, url: "/users/login" do |f| %>
	<div class="field">
		<%= f.label :name, "Select your name" %><br><%= f.collection_select(:id, @users, :id, :name) %>
	</div>
	<div class="actions"><%= f.submit "Sign In" %></div><br>

<% end %>

